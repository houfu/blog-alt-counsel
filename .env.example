# Environment Configuration for blog-alt-counsel
#
# This file documents the required environment variables for the GitOps workflow.
#
# OPTION 1: Use environment variables directly (recommended for CI/CD)
#   export GITHUB_USERNAME=your-username
#   export GITHUB_PAT=ghp_your_token
#   docker-compose up --build
#
# OPTION 2: Create a .env file (optional - for local development)
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up --build

# ============================================================================
# Docker GitOps Configuration (Required for Docker Compose)
# ============================================================================
# Repository URL for GitOps workflow (Docker will clone from this repo)
REPO_URL=https://github.com/houfu/blog-alt-counsel.git

# Git branch to use (defaults to 'main' if not specified)
BRANCH=main

# GitHub credentials for private repository access
GITHUB_USERNAME=your-github-username
GITHUB_PAT=ghp_your_personal_access_token_here

# ============================================================================
# Ghost Blog Configuration (Required for Ghost API Tools)
# ============================================================================
# Your Ghost site URL (without trailing slash)
GHOST_SITE_URL=https://yourblog.com

# Your Ghost Admin API Key (found in Ghost Admin > Integrations > Custom Integration)
# Format: key_id:secret_in_hex
GHOST_ADMIN_API_KEY=your_key_id:your_secret_hex_here

# Ghost API Version (usually v6.0 for modern Ghost installations)
GHOST_API_VERSION=v6.0
